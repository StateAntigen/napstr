# Shared languages

Both apps and the website use these offline JSON catalogues. `en.json` is extracted
with `npm run i18n:extract`; `npm run i18n:check` checks keys and placeholders.
Initial translations are machine-assisted; native-speaker improvements belong here.
`Verified · Complete` and `Downloading` deliberately remain English protocol statuses.
