# Website

Run `npm run website:build` at the repository root, then serve `website/dist`.
Edit `content/` for page copy and `template.mjs` for the shared layout.
Translations live in `shared/i18n/locales/`. Existing `.html` addresses stay valid.
